import ReferralCreationWizard from '@/components/network/ReferralCreationWizard';

export default function NewReferralPage() {
  // Demo mode - allow access without complex authentication
  const DEMO_MODE = true;

  return <ReferralCreationWizard />;
}