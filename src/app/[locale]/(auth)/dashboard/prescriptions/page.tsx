import UnifiedPrescriptionManager from '@/components/prescriptions/UnifiedPrescriptionManager';

export default function PrescriptionsPage() {
  // Demo mode - allow access without complex authentication
  const DEMO_MODE = true;

  return <UnifiedPrescriptionManager />;
}
